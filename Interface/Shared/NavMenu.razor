@using BusinessLogic
@using DepoQuick.Domain
@inject MemoryDataBase MemoryDataBase


<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">DepoQuick Menu</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        @if (_isUser)
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="index" Match="NavLinkMatch.All">
                    <span class="oi oi-home" aria-hidden="true"></span> Home
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="/Promotions">
                    <span class="oi oi-list-rich" aria-hidden="true"></span> Promociones
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="GestionOfReservs">
                    <span class="oi oi-list-rich" aria-hidden="true"></span> Gestión de reservas
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="DepositReserve">
                    <span class="oi oi-plus" aria-hidden="true"></span> Reservar
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="Register">
                    <span class="oi oi-plus" aria-hidden="true"></span> Registro
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="Statics">
                    <span class="oi oi-bar-chart" aria-hidden="true"></span> Estadisticas
                </NavLink>
            </div>
            /*
             <div class="nav-item px-3">
                <NavLink class="nav-link" href="Home">
                    <span class="oi oi-plus" aria-hidden="true"></span> Accion
                </NavLink>
            </div>
             */
           /*
             @if (UserLoggedIn())
            {
                <div class="nav-item px-3">
                    <NavLink class="nav-link">
                        <span class="oi oi-account-login" aria-hidden="true" @onclick="Logout"></span> Logout
                    </NavLink>
                </div>
            }
            */
        }
    </nav>
</div>

@code {
    private bool _isUser = true;
    private bool collapseNavMenu = false;
    private Controller _controller;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;
    
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
    protected override void OnInitialized()
    { 
        _controller = new Controller(MemoryDataBase);
        _isUser = _controller.GetActiveUser() != null; // metodo 
    }

    private bool UserLoggedIn()
    {
        return _controller.GetActiveUser() != null;
    }

    /*
     private void Logout()
    {
        MemoryDataBase.setActiveUser(null);
    }
     */
}